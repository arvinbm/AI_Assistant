# Optional ML dependencies for the local "multilingual" embedding backend.
# Only needed when EMBEDDING_BACKEND=multilingual.
# Install with: pip install -r requirements-ml.txt
sentence-transformers==3.3.1
